# Internal Documents

This directory contains implementation notes, generated planning packets, and agent-facing material.

The public GitHub root intentionally stays clean: `README.md` is the only root Markdown file.
